{% extends 'VorterixBackendBundle::layout.html.twig' %}
{% block title %}Vorterix :: Galerias{% endblock %}
{% block section_title %}Vorterix :: Galerias{% endblock %}
{% block content %}
<div class="row">
    <div class="col-xs-12">
        <div class="box">
            <div class="box-header">
                <h3 class="box-title">Listado de Galer&iacute;as</h3>
                <div class="box-tools">
                    <div class="input-group">                   
                        <div class="input-group-btn">
                            <a href="{{ url('VorterixBackendBundle_gallery_new') }}" class="btn btn-warning pull-right">Agregar Galer&iacute;a</a>
                        </div>
                    </div>
                </div>
            </div><!-- /.box-header -->
            <div class="box-body table-responsive no-padding">
                <table class="table table-hover">
                    <tr>
                        <th>ID</th>
                        <td></td>
                        <th>Galer&iacute;a</th>
 
                    </tr>
                    {% for gallery in galleries %}
                    <tr>
                        <td><a href="{{ url('VorterixBackendBundle_gallery_edit') }}/{{ gallery.id }}">{{ gallery.id }}</a></td>
                        <td><a href="{{ url('VorterixBackendBundle_gallery_edit') }}/{{ gallery.id }}">{{ gallery.name }}</a></td>
                        <td>
                            <div class="btn-group">
                                <button type="button" class="btn btn-default">Action</button>
                                <button type="button" class="btn btn-info dropdown-toggle" data-toggle="dropdown">
                                    <span class="caret"></span>
                                    <span class="sr-only">Toggle Dropdown</span>
                                </button>
                                <ul class="dropdown-menu" role="menu">
                                    <li><a href="{{ url('VorterixBackendBundle_gallery_edit') }}/{{ gallery.id }}">Editar</a></li>
                                    <li><a href="{{ url('VorterixBackendBundle_gallery_delete') }}/{{ gallery.id }}">Eliminar</a></li>
                                </ul>
                            </div>
                        </td>
                    </tr>
                    {% endfor %}
                </table>
            </div><!-- /.box-body -->
        </div><!-- /.box -->
    </div>
{% endblock %}