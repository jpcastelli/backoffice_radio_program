{% extends 'VorterixBackendBundle::layout.html.twig' %}
{% block title %}VorterixBackendBundle:Json:index{% endblock %}
{% block content %}
{% if message is defined %}
    <div class="alert alert-success alert-dismissable">
    <i class="fa fa-check"></i>
    <button type="button" class="close" data-dismiss="alert" aria-hidden="true">×</button>
    <b>Perfecto!</b> Json Creado
</div>
{% endif %}
<div class="col-md-4">
    <form action="{{ url('VorterixBackendBundle_Json_generate') }}" method="post">
        <div class="box-success box">
            <div class="box-header">  
            </div>
            <div class="box-body">
                <div class="form-group">
                    {% render url('VorterixBackendBundle_category_list') %}
                </div>
            </div>
        </div>
        
        <div class="box-success box">
            <div class="box-header"></div>
            <div class="box-body">
                <div class="form-group">
                    <button class="btn btn-success btn-lg">Generar Json</button>
                </div>
            </div>
        </div>
    </form>
    </div>
{% endblock %}
